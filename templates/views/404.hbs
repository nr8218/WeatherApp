<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/styles.css">
        <title>{{title}}</title>
    </head>
    <body>
        {{>header}}
        <p>{{errorText}}</p>
        {{>footer}}
    </body>
</html>