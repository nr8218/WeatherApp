<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/styles.css">
        <title>{{title}}</title>
    </head>
    <body>
        {{>header}}
        <h1>{{errorText}}</h1>
        {{>footer}}
    </body>
</html>